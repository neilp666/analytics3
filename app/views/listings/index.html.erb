<div class="row">
    <div class="center">
        <h1>Find a Great Job in the Web Analytics Industry</h1>
          <h3>We only show open analytics jobs to make your search easy</h3>
          <h3>Follow us on Twitter <span class="emphasis">@WorkinAnalytics</span> </h3>
          <br>
           <%= link_to "Post a job Today: $169 for 45 days", new_listing_path , class: "btn btn-danger btn-lg" %>
    </div>
</div>
<br/>

<table class="table table-hover">
    <% @listings.each do |listing| %>
      <tr>
        <td><h3><%= listing.company %></h3></td>
       
        <td><h3><%= link_to listing.job, listing %></h3></td>
        <td><h3><%= listing.created_at.strftime("%B %-d, %Y") %></h3></td>
        <td><%= link_to 'Show', listing %></td>
        <td><%= link_to 'Edit', edit_listing_path(listing) %></td>
        <td><%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

